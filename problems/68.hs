validAssignment :: [Int] -> Bool
--is the assignment to the 5-gon valid?
--for it to be valid, we want 10 to be used exactly once
--otherwise it'll be a 17 digit string
--so, 10 should be an external number

--we should consider all sets of 5 external numbers
--then assign the 